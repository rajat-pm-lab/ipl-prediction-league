{
  "buildCommand": "cd 02-prototype/ill-dashboard && npm install && npm run build",
  "outputDirectory": "02-prototype/ill-dashboard/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
